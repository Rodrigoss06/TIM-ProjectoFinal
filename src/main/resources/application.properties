# Base de datos (variables de entorno DB_URL, DB_USUARIO, DB_PASSWORD las sobrescriben)
db.url=jdbc:postgresql://localhost:5433/inventario_b
db.usuario=inventario
db.password=inventario_dev

# Pool de conexiones
db.pool.size=10
db.pool.timeout.ms=30000

# Sesiones y autenticación
sesion.expiracion.minutos=30
lockout.duracion.minutos=15
lockout.intentos.max=3

# Auditoría
auditoria.pagina.tamano=50
